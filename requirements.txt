# ── Gravity AI Bridge V16.0 PRO — Dependencias ───────────────────────────────

# HTTP / Proveedores cloud
httpx
requests
anthropic
google-generativeai
PyYAML>=6.0.2

# Seguridad — cifrado de API keys con DPAPI en Windows (obligatorio)
pywin32; sys_platform == 'win32'
pymysql

# RAG — Embeddings locales via ONNX (modelos en rag/models/)
onnxruntime
tokenizers
numpy
networkx
scipy

# Empaquetado Comercial (PyInstaller)
pyinstaller
pystray
Pillow
psutil
prometheus_client
moderngl

# Video Studio — TTS offline (Windows SAPI) y Transcripción
pyttsx3>=2.90
faster-whisper>=1.0.0

# Webscraping / MAI (Motor de Animacion Inteligente)
websocket-client>=1.7.0

# OBS Studio Control (Gravity OBS + Gravity Spark)
obsws-python>=1.7.0

# MCP (Model Context Protocol)
mcp

# Tests
pytest
pytest-mock

# Opcional: instalar si tienes API key de Firecrawl
# firecrawl-py

# Motor de Inferencia Nativo
llama-cpp-python>=0.2.79
huggingface-hub>=0.23.0

# Motor Infiltrador (Playwright Stealth)
playwright>=1.40.0
playwright-stealth>=1.0.6
