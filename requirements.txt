# ── Gravity AI Bridge V10.0 [Diamond-Tier Edition] — Dependencias ────────────────
# CLI / UI
aiohttp==3.10.10
colorama==0.4.6
PyYAML==6.0.2
gradio>=4.0.0
rich
pyfiglet
pyreadline3

# HTTP / Proveedores cloud
httpx
requests
anthropic
google-generativeai

# Configuración
# pyyaml ya está por PyYAML arriba

# Métricas Prometheus
prometheus_client

# RAG — Embeddings locales (obligatorio: instala si no tienes Ollama)
sentence-transformers
pypdf

# Seguridad — cifrado de API keys con DPAPI en Windows (obligatorio)
pywin32
pymysql

# Tests
pytest
pytest-mock

# MCP (Model Context Protocol)
mcp

# Empaquetado Comercial (Fase 2 — Build Instalable)
pyinstaller
pystray
Pillow
psutil
