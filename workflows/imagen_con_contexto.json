{
  "workflow_id": "imagen_con_contexto",
  "description": "Busca contexto en la web y el RAG local, genera un prompt optimizado con el LLM y produce una imagen de alta calidad.",
  "nodes": [
    {
      "id": "context",
      "type": "RAGQuery",
      "config": {
        "top_k": 3
      },
      "inputs": {
        "query": "{{topic}}"
      }
    },
    {
      "id": "prompt_opt",
      "type": "LLMQuery",
      "config": {
        "system": "Eres un experto en ingeniería de prompts para generación de imágenes. Convierte el tema dado en un prompt fotorrealista detallado para Stable Diffusion/Flux. Responde SOLO con el prompt, sin explicaciones.",
        "temperature": 0.8,
        "max_tokens": 200
      },
      "inputs": {
        "prompt": "Tema: {{topic}}\nContexto adicional: {{context.context}}\n\nGenera un prompt de imagen fotorrealista detallado en inglés:",
        "max_tokens": 500,
        "stop": [
          "\n\n\n"
        ]
      }
    },
    {
      "id": "image",
      "type": "ImageGenerator",
      "config": {
        "width": 1216,
        "height": 832
      },
      "inputs": {
        "prompt": "{{prompt_opt.text}}",
        "title": "{{topic}}"
      }
    }
  ]
}