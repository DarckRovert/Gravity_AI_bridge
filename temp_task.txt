Context: We are modifying the Gravity AI Bridge backend (`bridge_server.py` or FastAPI equivalent) and the frontend dashboard (`web/dashboard.html`) to include a GeoIP Security Tracker.

Task 1: Backend (`bridge_server.py`)
1. We need to create a global cache `_geoip_cache = {}` to store IP data and a queue/list `_recent_ips = []` to maintain the order of the last 20 IPs that hit any endpoint.
2. We need a background async task or an inline handler that takes an IP address. It must:
   - Check if the IP is already in `_geoip_cache`.
   - If not, make an HTTP GET request to `http://ip-api.com/json/{ip}`.
   - Store the resulting JSON (location, city, country, isp) in the cache.
   - If the IP is '127.0.0.1' or localhost, map it to a mock "Localhost / Gravity Core" location to avoid unnecessary API calls.
3. We need a new REST endpoint: `GET /v1/security/geoip`. This endpoint should return the state of the cache and the list of the most recent tracked IPs along with their geolocation data.

Task 2: Frontend (`web/dashboard.html`)
1. In the HTML dashboard, create a new Panel/Table (perhaps named "GeoIP Threat Map" or "Spy Tracker") styled consistently with the existing UI. 
2. Add a short Javascript script to fetch data from `/v1/security/geoip` every 10 seconds (polling).
3. Populate the panel with dynamically generated rows displaying the IP, Status, ISP, Country, City, and Lat/Lon.

REQUIREMENT:
Act as a Senior Architect System. Provide the full, precise Python implementation block that should be injected into `bridge_server.py`, AND the exact HTML/JS block that should be inserted into `dashboard.html`. DO NOT use placeholders like `// rest of the code`. Give the exact functions, imports, and variables. Output the results cleanly using Markdown code blocks.
